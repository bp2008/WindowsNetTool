# WindowsNetTool
A work-in-progress GUI application for Windows offering advanced networking capabilities such as DHCP and static IP coexistence on the same network interface, and the ability to switch network interfaces between types (Public, Private, Domain) even when Windows' own configuration GUI is missing that capability.
